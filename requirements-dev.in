# Development dependencies
# Include production requirements
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.23.0

# Linting and formatting
ruff>=0.4.0

# Type checking
mypy>=1.10.0
# Security scanning
bandit[sarif]>=1.7.0
pip-audit>=2.7.0

# Transitive dependencies (pinned to prevent Dependabot from removing them)
# setuptools is required by pbr, which is required by stevedore, which is required by bandit
setuptools>=80.0.0

# pip is pulled in transitively by pip-api (a pip-audit dep). Force >=26.1 so the
# lockfile doesn't pin the CVE-2026-3219-vulnerable pip 26.0.1.
pip>=26.1
